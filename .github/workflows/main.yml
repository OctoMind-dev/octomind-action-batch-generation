name: Batch Generation Action

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  batch-generation-action:
    runs-on: ubuntu-latest
    name: Batch Generation Test
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Corepack enable
        shell: bash
        run: |
          npm install -g corepack@latest
          corepack enable

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - uses: actions/cache@v4
        name: Setup pnpm cache
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-cache-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-cache-

      - name: install dependencies
        run: pnpm install

      - name: package
        run: pnpm run build

      - name: Batch Generation
        id: batch-generation
        uses: ./
        with:
          octomindBaseUrl: ${{ secrets.OCTOMIND_PREVIEW_URL }}
          testTargetId: '39218802-0800-4f88-9547-04c2593896c3'
          token: ${{ secrets.OCTOMIND_API_KEY }}
          prerequisiteId: 'a428cb8d-c938-4508-b303-903900177813'
